name: ClickUp Slack Bot (hourly)

on:
  schedule:
    - cron: "0 11-23 * * *"   # executa de 08h a 20h
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          pip install requests pytz

      - name: Run ClickUp Bot
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
          SLACK_CHANNEL_ID: ${{ secrets.SLACK_CHANNEL_ID }}
          CLICKUP_TOKEN: ${{ secrets.CLICKUP_TOKEN }}
          CLICKUP_LIST_IDS: ${{ secrets.CLICKUP_LIST_IDS }}
        run: |
          echo "🕐 Iniciando execução em $(date -u) UTC / $(TZ='America/Sao_Paulo' date) BR"
          python bot_clickup_produto_horario.py
          echo "✅ Finalizado em $(date -u) UTC / $(TZ='America/Sao_Paulo' date) BR"
